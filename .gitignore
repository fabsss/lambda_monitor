.pio/
.vscode/

# Generated by tools/embed_frontend.py (pre: build script) from web/*.
# Tracking this caused CI checkouts to look "dirty" whenever the committed
# copy's cache-hash comment drifted from a fresh regeneration, even with
# byte-identical embedded content — see CLAUDE.md.
src/frontend_assets.h
